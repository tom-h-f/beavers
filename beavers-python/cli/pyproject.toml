[project]
name = "cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "gridworld",  # Reference to workspace member
]

[tool.uv.sources]
gridworld = { workspace = true }

